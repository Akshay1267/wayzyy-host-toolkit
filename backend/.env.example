# Anthropic Claude API Key for Wayzyy Host Toolkit
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# (Optional) Anthropic Workspace ID (required only if using identity-linked / organization workspace keys)
# ANTHROPIC_WORKSPACE_ID=wrkspc_...

# Server Port (default: 5000)
PORT=5000
